<h2>Add a cocktail:</h2>
    <%= simple_form_for @cocktail do |f| %>
      <%= f.input :name, placeholder: "Add a name" %>
      <%= f.input :photo, as: :file %>
      <%= f.submit "Add", class: "btn btn-gradient" %>
    <% end %>
    
